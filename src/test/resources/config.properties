# Base configuration for the Bookstore API test suite.
# Any value here can be overridden at runtime via a JVM system property
# (e.g. -DbaseUri=...) or an environment variable (e.g. BASE_URI=...).

baseUri=https://fakerestapi.azurewebsites.net
basePath=/api/v1
timeoutMs=15000
